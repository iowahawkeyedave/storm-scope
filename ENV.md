# Storm Scope — Environment Variables

No environment variables required for v1.

This is a static site with no backend, no database, and no external API calls.

## Future v2 Variables (if needed)

```bash
# AI Vision for chart parsing
OPENAI_API_KEY="sk-..."

# Weather data APIs
NOAA_API_KEY="..."
STORM_PREDICTION_CENTER_API_KEY="..."

# Analytics
GA_TRACKING_ID="G-XXXXXXXXXX"
```
